image: Ubuntu

environment:
  nodejs_version: "14"

build: off

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install

build_script:
  - npm run build

test_script:
  - npm test
  